# 构造最小补码整数

补全 `tmin`，返回 32 位二进制补码能表示的最小整数。

```c
tmin() == -2147483648
```

## 学习范围

- 32 位整数的补码表示
- 符号位与左移
- 有符号整数边界

## 代码要求

- 合法操作符：`!`、`~`、`&`、`^`、`|`、`+`、`<<`、`>>`
- 最多 4 个操作符
- 整数常量只能使用 `0` 到 `255`
- 只补全函数体；题目不会自动检查操作符限制

## 提交说明

完成 `tmin` 函数并通过 Clings 测试。自行检查实现是否符合代码要求。
