# 用与非实现异或

异或会在两个对应位不同时得到 1。现在只允许使用 `~` 和 `&`，补全 `bitXor`，让它返回 `x ^ y` 的结果。

```c
bitXor(4, 5) == 1
bitXor(7, 7) == 0
```

## 学习范围

- 真值表与布尔代数
- 德摩根律
- 按位运算和逻辑运算的区别

## 代码要求

- 合法操作符：`~`、`&`
- 最多 14 个操作符
- 只补全函数体；题目不会自动检查操作符限制

## 提交说明

完成 `bitXor` 函数并通过 Clings 测试。自行检查实现是否符合代码要求。
