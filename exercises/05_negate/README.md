# 用补码取相反数

补全 `negate`，返回 `x` 的相反数。

```c
negate(1) == -1
negate(-7) == 7
```

## 学习范围

- 补码的按位取反与加一
- 有符号整数范围
- `INT_MIN` 的边界行为

## 代码要求

- 合法操作符：`!`、`~`、`&`、`^`、`|`、`+`、`<<`、`>>`
- 最多 5 个操作符
- 整数常量只能使用 `0` 到 `255`
- 只补全函数体；题目不会自动检查操作符限制

## 提交说明

完成 `negate` 函数并通过 Clings 测试。自行检查实现是否符合代码要求。
